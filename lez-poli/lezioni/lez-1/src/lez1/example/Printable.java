package lez1.example;

public interface Printable { //le interfacce hanno solo metodi abstract
    public void print(); //è un metodo abstract ma è ovvio, quindi non serve specificarlo.
}//la promessa che una classe implementerà questo metodo (vedi in classe Figura)
